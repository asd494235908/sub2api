# AGENTS.md

## 1. 仓库规则读取

开始任何修改前，必须先读取并遵守根目录 `AGENTS.md`。

如果目标目录后续新增了更细分的 `AGENTS.md`，则必须同时遵守根目录规则和目标目录规则。规则冲突时，优先遵守用户本次明确指令；其次遵守更贴近目标目录的规则；最后遵守根目录通用规则。

## 2. 范围控制

只允许修改用户明确要求的内容。

禁止：

- 无关改动
- 顺手重构
- 顺手修复无关问题
- 擅自扩大任务范围
- 格式化或整理与当前任务无关的文件

如果发现现有结构、已有改动或任务目标之间存在冲突，必须先说明冲突和风险，再继续处理。

## 3. 先计划后执行

中等复杂度及以上任务，必须先给出计划，再实施修改。

计划至少应包含：

1. 改动目标
2. 涉及目录
3. 涉及文件
4. 是否影响文档、接口或数据结构
5. 验证方式
6. 风险点

简单任务可以直接执行，但仍必须保持改动范围清晰、可验证。

## 4. 验证规则

只要发生实际修改，就必须执行与改动范围匹配的验证。

要求：

- 文档修改要做读取确认和差异确认
- 单目录代码修改要做该目录匹配的测试或检查
- 跨目录修改要说明各目录验证范围
- 如验证无法执行，必须说明原因和剩余风险

禁止：

- 不验证就标记完成
- 用理论分析代替执行结果
- 只验证局部但声称全量通过

## 5. Git 与 worktree 规则

未经用户明确要求，禁止执行以下操作：

- 创建新分支
- 切换分支
- 创建或切换 worktree
- 删除分支
- 合并分支
- rebase
- reset
- 修改远程仓库或推送到远程

默认在当前分支和当前工作区内完成任务。

提交代码时，提交信息必须使用中文，并明确说明本次提交实现了什么功能，或修复了什么代码问题。

如果用户明确要求进行分支、worktree、合并、rebase、reset、推送等 Git 操作，必须先确认目标分支名、操作目的和可能影响范围，除非用户已经在同一条请求中明确给出这些信息。

## 6. 现有改动

遇到已有未提交改动时，默认视为用户改动。

禁止：

- 回滚用户改动
- 覆盖用户改动
- 擅自暂存、提交或格式化用户改动
- 将无关未提交改动混入当前任务结果

如果必须修改已有改动所在文件，必须先确认当前改动内容，并尽量只追加或调整与本任务直接相关的部分。

## 7. 文档与接口一致性

涉及文档、接口、配置、数据库结构或跨端联动时，必须保持事实来源一致。

禁止脱离现有实现和文档另起一套口径。发现文档、接口或实现不一致时，应先说明不一致点和建议处理方式，再按用户确认的范围执行。

## 8. 开发规则

实施代码时，必须遵守以下轻量开发规范：

- 文件职责保持单一
- 遵循项目已有目录结构、命名和实现风格
- 禁止提交临时代码、调试日志、测试地址、无用途注释
- 禁止把密钥、私有地址或环境差异硬编码进代码
- 新增模块或目录时，必须确认其职责与现有结构一致

## 9. 完成规则

完成任务时，必须说明：

- 实际修改了什么
- 执行了哪些验证
- 是否存在未执行的验证或剩余风险

不得把未验证、部分完成或存在阻塞的任务描述为已完全完成。
